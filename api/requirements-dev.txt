# Test + dev dependencies. NOT installed in production images.
# Install with: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
pytest==8.2.0
# 0.24+ required for loop_scope on @pytest_asyncio.fixture; this pin
# unblocks the session-scoped pg_pool fixture from the DB integration
# tests (without it, tests get a "Future attached to a different loop"
# error when sharing an asyncpg pool across function-scoped tests).
pytest-asyncio>=0.24
# DB integration tests use a real PostgreSQL container, spawned per
# test session via testcontainers. **We use Podman, not Docker**
# (per the AAC stack convention) — the integration conftest fixture
# auto-detects a running podman-machine socket on macOS or the
# rootless podman socket on Linux and points testcontainers at it
# via DOCKER_HOST. Marker `@pytest.mark.integration` gates these so
# default `pytest` only runs unit tests; integration tests
# automatically skip if no container runtime is reachable.
testcontainers[postgres]==4.7.1
