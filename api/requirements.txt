fastapi==0.111.0
uvicorn[standard]==0.29.0
asyncpg==0.29.0
pydantic[email]==2.7.1
pydantic-settings==2.2.1
httpx==0.27.0
python-multipart==0.0.9
bcrypt==4.2.0
pyotp==2.9.0
pypdf==4.3.1
python-docx==1.1.2
beautifulsoup4==4.12.3
Jinja2==3.1.4
PyNaCl==1.5.0
# Rate-limit primitives (counters + moving-window). Exposed as a
# FastAPI Depends factory in src/core/rate_limit.py; no slowapi.
limits==5.8.0

# Structured logging — X-Request-ID middleware + JSON formatter.
# Both were imported in main.py / core/logging.py by PR #40 but
# never declared, so CI couldn't install them.
asgi-correlation-id==4.3.4
python-json-logger==3.3.0

# PDF generation for /reports/download. Pure-Python — no system deps
# (in contrast to weasyprint's cairo/pango requirements). Output is
# functional rather than typographically beautiful; for v1 the
# evidence content matters more than the polish.
reportlab==4.2.5

# Prometheus exposition — counters + histograms exposed at GET /metrics.
# Pure-Python, single dep. The fastapi-instrumentator wrapper would
# pull more transitive deps for marginal value over a thin middleware.
prometheus-client==0.21.0
